#include <gtest/gtest.h>

#include "clf/MinimizeQuadraticCost.hpp"

using namespace clf;

TEST(MinimizeQuadraticCostTests, Dense) {
  EXPECT_TRUE(false);
}

TEST(MinimizeQuadraticCostTests, Sparse) {
  EXPECT_TRUE(false);
}
